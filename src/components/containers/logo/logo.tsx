const Logo = ({ className }: { className: string }) => {
  return (
    <span className={`${className} font-playwrite font-semibold tracking-wider`}>
      HonourableMemberGPT
    </span>
  );
};

export default Logo;
